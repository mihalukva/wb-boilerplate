import { Route } from 'types/router';
import Page from '.';

const route: Route = {
  name: '<%= name %>',
  path: '/<%= path %>',
  title: '<%= title %>',
  Component: Page,
};

export default route;
